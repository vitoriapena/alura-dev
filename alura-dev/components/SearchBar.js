function SearchBar() {
    return (
        <input type="search"
            placeholder="Busque por algo"
            className="w-full bg-white bg-opacity-25 rounded p-2 dark:text-white"
        />
    )
}

export default SearchBar
